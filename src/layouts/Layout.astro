<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/iconApp.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Minimalist Portfolio</title>
    <script>
      if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
        document.documentElement.classList.add("dark");
      }
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  html {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      "Courier New",
      Courier,
      monospace;
    background: white;
    letter-spacing: -0.025rem;
  }

  .title-section {
    color: black;
  }

  .title-in-section {
    color: #111;
  }

  .title-in-section .position {
    color: #111;
  }

  .title-in-section:hover {
    text-decoration: underline;
  }

  .hero {
    color: #222;
  }

  .work-period {
    color: #555;
    font-size: 0.85rem;
    min-width: 102px;
    cursor: default;
  }

  #hero-loc {
    color: #666;
    display: flex;
    align-items: center;
    gap: 0.25rem;
    font-size: 0.85rem;
    letter-spacing: -0.05rem;
  }

  .desc {
    color: #666;
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
    text-wrap: pretty;
  }

  .title-category {
    color: black;
  }

  body,
  figure {
    margin: 0;
    padding: 0;
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  h1,
  h2,
  h3,
  h4 {
    margin: 0;
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }

  .print {
    display: none !important;
  }

  .dark {
    background-color: #1a1a2e;
    color: white;
  }

  .dark .title-section {
    color: white;
  }

  .dark .title-in-section {
    color: white;
  }

  .dark .title-in-section .position,
  .dark .work-period {
    color: white;
  }

  .dark .hero,
  .dark #hero-loc,
  .dark .desc {
    color: white;
  }

  .dark span {
    color: white !important;
  }

  .dark .skillList {
    color: black !important;
  }
  .dark .title-category {
    color: white;
  }

  .dark #normal-footer {
    background-color: #1a1a2e;
  }

  .dark kbd {
    background-color: transparent !important;
  }

  @media print {
    .no-print {
      display: none !important;
    }

    .print {
      display: block !important;
      color: #1a1a2e !important;
    }

    .hero,
    #hero-loc,
    .desc,
    .work-period,
    .title-section,
    .title-category {
      color: black !important;
    }

    .title-in-section {
      color: #111 !important;
    }

    article h4 {
      color: #222 !important;
    }

    astro-dev-toolbar {
      display: none !important;
    }

    article {
      break-inside: avoid;
    }
  }
</style>
